Update the landing page with the following improvements while keeping the existing layout, component structure, and functionality unchanged.

1. Carousel Image Placement

* Ensure all carousel images are perfectly centered and aligned.
* Images must fully fit inside the carousel without stretching or distortion.
* Maintain proper aspect ratio.
* Use styling such as object-fit: cover or contain so images display cleanly.
* Images should fill the available carousel area nicely while keeping the container size unchanged.

2. Increase Image Presence

* Make the carousel images appear slightly larger inside the container.
* Do NOT increase the carousel background container width.
* Only adjust the image styling (such as width, max-width, or scale) so the images appear more prominent.

3. Replace Carousel Images
   I will upload 6 images. Organize them in the following order:

First 3 images → Inauguration
Next 3 images → Events

The carousel should display them in this exact order.

4. Image Structure

* Do NOT create new folders.
* Use the same assets/images folder already used in the repository.
* Keep the same naming structure if possible so imports do not break.

Example order:
image1.jpg → inauguration
image2.jpg → inauguration
image3.jpg → inauguration
image4.jpg → events
image5.jpg → events
image6.jpg → events

5. Maintain UI and Functionality

* Do NOT change the carousel component logic.
* Do NOT remove the decorative line below the carousel.
* Keep navigation buttons and indicators working normally.
* Only improve image placement and styling.

6. Update Button Names
   Update the button text across the landing page as follows:

Navbar:

* Change **"Join Us"** → **"Plug In ⚡"**

Hero Section:

* Change **"Explore Events"** → **"Ignite Events 🔥"**
* Change **"Learn More"** → **"Uncover"**

7. Update Icon for the "Uncover" Button

* Replace the current icon used in the "Learn More" button.
* Use an icon that matches the meaning of **Uncover** such as:

  * search/discover icon
  * magnifying glass
  * reveal/insight style icon
* The icon should animate slightly on hover (small slide or glow effect).

Goal:
Improve the carousel image display and update the button texts to a more modern and tech-themed style while keeping the overall design, layout, and functionality intact.
